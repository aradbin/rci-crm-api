1. Customer: Add optional contact on db --> done
2. WhatsApp: Add whatsapp_settings table with number and user_id --> done
3. WhatsApp: Add whatsapp_messages table to store sent and received messages --> done
4. Task: Add time_log (json) column in tasks table [{ type: string, status: string, timestamp: timestamp }]. No need to create other endpoint. we'll use task update service --> done
5.
