export enum TaskStatus {
  CREATED = 'created',
  ASSIGNED = 'assigned',
  FINISHED = 'finished',
  DELIVERED = 'delivered',
}

export enum TaskProgressStatus {
  NONE = 'none',
  TODO = 'to_do',
  DOING = 'doing',
  DONE = 'done',
}
